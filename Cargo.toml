[package]
name = "pci-topo"
version = "0.1.0"
edition = "2024"
description = "PCI / NUMA topology inspector for HPC systems"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
